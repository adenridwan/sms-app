name: sms_mobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.5.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.5.1
  # HTTP client
  dio: ^5.5.0
  # Secure token storage (Keystore / Keychain)
  flutter_secure_storage: ^9.2.2
  # Declarative routing + auth gate
  go_router: ^14.2.0
  # Preferensi ringan (mode tema, antrean offline)
  shared_preferences: ^2.3.2

  # Verifikasi kredensial offline (PBKDF2-HMAC-SHA256).
  crypto: ^3.0.3

  # QR presensi milik sendiri di tab "Absensi Saya".
  qr_flutter: ^4.1.0
  # Pemindai QR (kamera) — versi 3.x kompatibel dengan Flutter Gradle Plugin 3.24
  mobile_scanner: ^3.5.7
  # Ambil gambar dari galeri (untuk scan QR dari screenshot)
  image_picker: ^1.0.7
  # Lokasi GPS (geofence absensi) — 10.x kompatibel dengan toolchain ini
  geolocator: ^10.1.1

  # Deep linking untuk QR provisioning (smsapp://provision?token=xxx)
  app_links: ^6.3.2

  # Offline-first SQLite database dengan compile-time safety
  drift: ^2.14.0
  sqlite3_flutter_libs: ^0.5.18
  path_provider: ^2.1.1
  path: ^1.8.3

  # Heartbeat service — info device untuk monitoring
  device_info_plus: ^10.1.0
  package_info_plus: ^8.0.0
  connectivity_plus: ^6.0.3
  network_info_plus: ^5.0.3
  uuid: ^4.6.0
  zxing2: ^0.2.4
  image: ^4.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0

  # Drift code generator
  drift_dev: ^2.14.0
  build_runner: ^2.4.7

  # Pembangkit ikon peluncur dari satu berkas sumber.
  flutter_launcher_icons: ^0.14.1
  qr: ^3.0.2

# Ikon peluncur SMS-APP. Regenerasi setelah mengubah berkas di assets/branding:
#   flutter pub run flutter_launcher_icons
#
# `image_path` dipakai untuk ikon legacy (Android < 8, iOS, web) — logo penuh
# tepi ke tepi. Android 8+ memakai adaptive icon: sistem memotong bentuknya
# (bulat/squircle) sesuai peluncur, jadi `..._foreground` adalah logo yang sudah
# diperkecil supaya jatuh di dalam *safe zone*. Perhitungannya: paket ini
# membungkus foreground dengan inset 16% (menyisakan 68% kanvas), sedangkan safe
# zone adaptive icon 66/108 = 61%; berkasnya karena itu digambar pada skala
# 0,61/0,68 = 0,90 (lihat catatan di assets/branding).
# Warna latar disamakan dengan warna dasar logo agar sambungannya tak terlihat.
flutter_launcher_icons:
  image_path: assets/branding/logo.png
  android: true
  min_sdk_android: 21
  adaptive_icon_background: "#1B2340"
  adaptive_icon_foreground: assets/branding/logo_foreground.png
  ios: true
  # iOS menolak ikon bertransparansi; ratakan ke warna dasar logo.
  remove_alpha_ios: true
  background_color_ios: "#1B2340"
  web:
    generate: true
    background_color: "#1B2340"
    theme_color: "#1B2340"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Archivo — font pada rujukan desain. Dibundel (bukan diunduh runtime) supaya
  # tampilan tetap sama saat perangkat offline, keadaan yang memang lumrah di
  # aplikasi ini. Berkasnya adalah instance statis per bobot, dihasilkan dari
  # font variabel, agar `fontWeight` bekerja normal tanpa `fontVariations`.
  fonts:
    - family: Archivo
      fonts:
        - asset: assets/fonts/Archivo-Regular.ttf
          weight: 400
        - asset: assets/fonts/Archivo-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Archivo-Bold.ttf
          weight: 700
        - asset: assets/fonts/Archivo-ExtraBold.ttf
          weight: 800

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
